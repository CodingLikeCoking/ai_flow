# Contributing

## Development flow

1. Add or update a test first.
2. Run the focused test red.
3. Implement the minimal change.
4. Run `npm run test` and `npm run typecheck`.

## Scope

- Keep filesystem output deterministic.
- Preserve backward compatibility for the MCP tool names and resource URIs.
- Prefer local-first defaults over hosted dependencies.
